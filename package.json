{
  "name": "material-mock-problem",
  "private": true,
  "version": "0.0.1",
  "description": "services and factories are bound by name to angular itself, so services or factories from different modules can be overridden",
  "repository": "",
  "license": "MIT",
  "devDependencies": {
    "grunt": "^0.4.1",
    "grunt-contrib-connect": "^0.7.1",
    "grunt-contrib-watch": "^0.6.1",
    "load-grunt-tasks": "^0.4.0"
  },
  "scripts": {
    "postinstall": "bower install"
  }
}
